export class Ingredient {
  // public name: string;
  // public amount: number;
  //
  // constructor(name: string, amount: number) {
  //   this.name = name;
  //   this.amount = amount;
  // }

  // Below will do the same as above.Declares two attribute and assign the values which are passed to them.
  constructor(public name: string, public amount: number) { }
}
